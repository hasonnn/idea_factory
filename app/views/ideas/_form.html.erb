<%= form_for @idea do |f| %>
    <div class="padding">
        <%= f.label :title %>
        <%= f.text_field :title %>
        <br>
        <%= f.label :body %>
        <%= f.text_field :body %>
    </div>
    <div class="padding">
        <%= f.submit %>
    </div>
<% end %>